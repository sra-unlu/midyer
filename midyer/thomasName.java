import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class thomasName here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class thomasName extends names
{
    /**
     * Act - do whatever the thomasName wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public thomasName(){
        GreenfootImage x = getImage();
        x.scale(295, 52);
        setImage(x);
    }
}
