import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class polymorphisimUnit here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class polymorphisimUnit extends spells
{
    /**
     * Act - do whatever the polymorphisimUnit wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public polymorphisimUnit(){
        GreenfootImage polymorphisimUnit = new GreenfootImage("polymorphisimUnit.png");
        polymorphisimUnit.scale(72, 90);
        setImage(polymorphisimUnit);
    }
}
